name = "fakestore-api"
main = "src/index.ts"
compatibility_date = "2024-09-01"
no_build = true

# --- D1 Database ---
[[d1_databases]]
binding = "DB"
database_name = "fakestore-db"
database_id = "local-dev-db-id"

# --- Cache product API responses ---
# Using CF Cache API (caches.default), no binding needed

# --- JWT Secret set via wrangler secret put JWT_SECRET ---
# [vars] for non-secret env vars only
